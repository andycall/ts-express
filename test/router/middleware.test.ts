/// <reference types="node" />

import express from '../../src/express'

describe("middleware", () => {
    const app = express();
    const router = app.router;

    it('use middleware before route match', () => {
        
    });
});